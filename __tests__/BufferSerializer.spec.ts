import { BufferSerializer} from '../src/serializers/BufferSerializer';

describe('Check BufferSerializer', () => {
  it('check for basic usuage', () => {
    // const serializer = new BufferSerializer(1);
    // expect(serializer.version).toBe(1);
  });
});
